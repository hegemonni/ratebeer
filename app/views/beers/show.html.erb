<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @beer.name %>
</p>

<p>
  <strong>Style:</strong>
  <%= @beer.style %>
</p>

<p>
  <strong>Brewery:</strong>
  <%= @beer.brewery_id %>
</p>

<p>
	<% if @beer.ratings.empty? %>
		Beer has not yet been rated!
	<% else %>
		Beer has <%= pluralize(@beer.ratings.count, "rating") %>, the average rating is <%= @beer.average_rating.to_s %>.
	<% end %>
</p>

<p>Here you can find a <%= link_to 'list of breweries', breweries_path %></p>

<%= link_to 'Edit', edit_beer_path(@beer) %> |
<%= link_to 'Back', beers_path %>
