<p id="notice"><%= notice %></p>

<p>
  <h2><%= @user.username %></h2>
  <% if @user.ratings.empty? %>
	You have not made any ratings yet!
  <% else %>
	has made <%= pluralize(@user.ratings.count, "rating") %> the average rating is <%= @user.average_rating.round(2).to_s %>.
	<h3>ratings</h3>
	<ul>
	  <% @user.ratings.each do |rating| %>
        <li>
           <%= rating.to_s %> 
           <% if @user == current_user %>
              <%= link_to 'delete', rating_path, method: :delete, data: { confirm: 'Are you sure?' } %>
              <% end %>
        </li>
	<% end %>
  <% end %>
</p>

<p>
<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Destroy', user_path(@user), method: :delete, data: { confirm: 'Are you sure?' } %> |
<%= link_to 'Back', users_path %>
</p>